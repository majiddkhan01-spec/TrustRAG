services:
  - type: web
    name: trustrag
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn trustrag.api.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: GROQ_API_KEY
        sync: false